apiVersion: v1
kind: Service
metadata:
  name: omniagent-backend
  labels:
    app: omniagent-backend
spec:
  selector:
    app: omniagent-backend
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
  type: ClusterIP
